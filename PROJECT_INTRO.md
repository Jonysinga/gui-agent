# UI Agent 项目介绍

## 业务背景

本项目目标是构建一个能够自主完成网页任务的 AI Agent，用于自动化执行用户在浏览器中的操作（如搜索、筛选、填表、预订等）。

本项目采用**纯视觉方案**——Agent 只通过截图感知界面状态，不依赖 DOM 树或页面源码，因此对任意网页具备较强的泛化能力，不受前端框架或页面结构变化影响。

---

## 技术架构

### 双模型设计：Analyst + Executor

```
用户目标（自然语言）
        │
        ▼
┌───────────────────────────────┐
│  Analyst（大脑）               │
│  模型：Gemini 3 Pro            │
│  职责：感知页面状态、规划步骤、    │
│        校验上一步执行结果        │
│  输出：自然语言指令              │
└──────────────┬────────────────┘
               │
               ▼
┌───────────────────────────────┐
│  Executor（小脑）              │
│  模型：GUI-Owl-1.5-8B（本地）   │
│  职责：读取截图，将指令转化为     │
│        具体坐标和动作           │
│  输出：Action: click(x, y)     │
└──────────────┬────────────────┘
               │
               ▼
        操作系统执行
```

- **Analyst** 负责"思考"：每步接收前后两张截图，对比变化评估上一步是否成功，再规划下一步
- **Executor** 负责"执行"：只做视觉定位，输出单个动作，不做任何决策

### 坐标系统

Executor 输出 0–1000 归一化坐标，经两次映射后执行：

```
模型输出 (0~1000)  →  逻辑坐标 (1920×1080)  →  物理像素 (3840×2160)
       ×1920/1000                                    ×DPI scale 2.0
```

### Prompt 管理

所有 prompt 集中配置在 `prompt2.yaml`，分四部分：

| 部分 | 内容 |
|------|------|
| `analyst_prompt` | Analyst 系统 prompt，含决策逻辑、高危组件知识、输出格式规范 |
| `executor_prompt` | Executor 系统 prompt，含动作空间定义、已知坐标偏移修正规则 |
| `task_template` | 每轮输入模板，注入任务目标、历史轨迹、组件说明、动态警告 |
| `warnings` | 连续重复操作/过度滚动时自动注入的警告片段 |

---

## 准确率

在三个网站共 60 个任务上测试，整体 click 准确率约 **90%–93%**，主要误差来源为小按钮坐标估算偏差和紧邻元素视觉干扰，已通过 prompt 规则部分修复。
